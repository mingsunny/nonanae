# 디자인 시스템 — 노나내(정산일지)

## 제품 컨텍스트

- **무엇**: 여행 등에서 친구끼리 함께 쓴 돈을 그룹으로 관리하고 최소 송금으로 정산해주는 앱
- **누구를 위해**: 20-30대 친구 그룹 — "정산"이라는 다소 껄끄러울 수 있는 행위를 가볍고 즐겁게 만드는 것이 핵심
- **브랜드**: "노나내" (나눠먹다/나누다에서 착안, 저장소명 `nonanae`) — 로고는 초록색 지폐(banknote) 모티프에 두껍고 둥근 스티커 스타일 타이포, 원(₩) 코인 배지, 스파크 장식으로 구성됨
- **레퍼런스**: `assets/logo.png` (사용자 제공 로고, 저장소에 추가 필요)

## 기억에 남길 한 가지

**"정산이 이렇게 귀엽고 산뜻할 수 있구나."** 돈 이야기가 주는 긴장감을 지폐 캐릭터, 코인, 스파크 같은 장난스러운 시각 요소로 눌러준다. 데이터가 밀도 있게 나오는 화면(지출 목록, 정산 표)은 절제하고, 감정적으로 중요한 순간(첫 그룹 생성, 정산 완료, 빈 상태)에만 브랜드 일러스트 톤을 강하게 쓴다.

## 아트 디렉션

- **방향**: Playful / Toy-like — 로고의 스티커·뱃지 스타일을 UI 전반의 톤으로 확장
- **장식 수준**: Intentional — 일러스트는 감정적 순간에 집중 투입, 리스트/표 화면은 타이포와 여백 위주로 절제
- **재질감**: 종이(지폐) 질감의 따뜻한 크림 배경 + 두꺼운 아웃라인 + 소프트 드롭섀도우(스티커 느낌)

### 안전한 선택 (카테고리 기본기)

- 모바일 앱 표준 패턴 유지 — 하단 탭바, 카드형 리스트, 알약(pill) 버튼. 정산 앱 사용자가 이미 기대하는 조작 방식을 벗어나지 않음
- 숫자는 항상 `tabular-nums`로 정렬 — 금액 비교가 핵심 태스크이므로 가독성 타협 없음

### 리스크 (브랜드가 자기 얼굴을 갖는 지점)

1. **초록을 진짜 브랜드 컬러로 승격**: 기존 프로토타입은 청록(teal)을 포인트로 썼지만, 로고가 초록 지폐 컨셉으로 확정된 이상 브랜드 그린을 1순위 색으로 올림. 부수 효과: 기존에 "받을 돈(긍정)"을 표시하던 초록 계열과 브랜드 컬러가 자연스럽게 겹쳐 "초록 = 좋은 신호"라는 의미가 강화됨
2. **청구서체가 아니라 스티커체**: 정산 앱은 보통 은행 앱처럼 차갑고 정직한 인상을 주려 하지만, 이 브랜드는 반대로 두껍고 둥근 타이포 + 코인 캐릭터로 "친구끼리 쓰는 앱"이라는 인상을 우선함. 대가: 신뢰감보다 친근함을 택한 것이므로, 실제 송금 관련 화면(계좌 노출 등)에서는 장식을 걷어내고 타이포/여백만으로 신뢰감을 보완해야 함

## 타이포그래피

| 역할 | 폰트 | 비고 |
|---|---|---|
| 디스플레이 / 헤드라인 | **Do Hyeon** (Google Fonts) | 로고 레터링과 같은 결의 두껍고 둥근 한글 디스플레이체. 화면 타이틀, 금액 강조, 온보딩 카피에 사용. Google Fonts로 바로 로드 가능해 도입 비용이 낮음 |
| 본문 / UI | **Pretendard** | 기존 프로토타입에서 이미 쓰던 서체 유지 — 작은 글자에서도 가독성이 좋아 리스트/폼에 적합 |
| 숫자 / 표 | **Pretendard**, `font-variant-numeric: tabular-nums` | 금액·정산 표에서 자릿수 정렬 |
| 로딩 전략 | Do Hyeon은 Google Fonts CDN. Pretendard는 자체 호스팅 또는 jsDelivr `pretendard` 패키지 |

**스케일** (rem 기준, base 16px)
- Display XL: 2rem / 800 — 그룹 총액, 정산 완료 화면
- Display L: 1.5rem / 800 — 화면 타이틀
- Title: 1.0625rem / 700 — 카드 제목, 인원명
- Body: 0.9375rem / 500 — 본문, 리스트 항목
- Caption: 0.8125rem / 500 — 보조 텍스트, 타임스탬프
- Label: 0.75rem / 700, letter-spacing 0.02em, uppercase 아님(한글) — 카테고리/뱃지

## 컬러

로고에서 추출한 지폐 그린을 브랜드 컬러로 승격하고, 기존 프로토타입의 따뜻한 종이 배경을 계승한다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--paper` | `#F7F2E6` | 앱 배경 (지폐 종이 질감) |
| `--card` | `#FFFFFF` | 카드/시트 표면 |
| `--ink` | `#182A20` | 본문 텍스트 (초록 언더톤의 다크 뉴트럴) |
| `--ink-soft` | `#5B6B5F` | 보조 텍스트 |
| `--line` | `#E4DFCC` | 구분선, 아웃라인 |
| `--brand` | `#1F8A50` | 1차 브랜드 그린 — 버튼, 활성 탭, 로고 컬러 |
| `--brand-deep` | `#175C38` | 브랜드 그린 눌림/강조 — 헤드라인 텍스트, 프레스 상태 |
| `--brand-light` | `#DCF3E3` | 브랜드 틴트 — 뱃지/칩 배경 |
| `--mint` | `#4EAD6D` | 2차 그린 — 코인/일러스트 하이라이트 |
| `--amber` | `#E3A23C` | 경고/카테고리 포인트 (기존 유지) |
| `--brick` | `#C1443C` | 부정 금액("보낼 돈"), 에러, 삭제 |

> `--brand`와 "받을 돈(긍정 잔액)" 표시 색을 같은 그린 계열로 통일 — 브랜드 인지와 긍정적 금전 신호가 시각적으로 강화되도록 의도한 선택.

**다크 모드**: 이 앱은 "따뜻한 지폐 종이" 메타포가 브랜드의 핵심이라, 지금 단계에서는 라이트(종이) 테마 하나로 고정하고 다크 모드는 만들지 않는다. 추후 다크 모드가 필요해지면 `--paper`를 짙은 올리브-그린 계열(`#14201A` 등)로, `--ink`를 크림 계열로 뒤집는 방향을 검토.

## 스페이싱 / 레이아웃

- **베이스 단위**: 4px
- **밀도**: Comfortable — 리스트 밀도가 높은 화면(지출 내역)도 한 행에 최소 44px 터치 영역 확보
- **스케일**: 2xs(2) xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48)
- **Border radius**: sm 8px(입력 필드) / md 12px(버튼) / lg 16px(카드, 기존 프로토타입 `--radius` 유지) / full 9999px(알약 버튼, 코인 뱃지, 칩)
- **그림자**: 스티커 느낌의 소프트 드롭섀도우 1종만 사용 — `0 6px 16px -6px rgba(23,92,56,.18)` (브랜드 그린 틴트 섀도우, 뜬금없는 회색 섀도우 지양)

## 모션

- **기본**: Minimal-functional — 화면 전환, 탭 전환은 절제된 슬라이드/페이드
- **감정적 순간에는 Toy-like로 전환**: 정산 완료, 첫 그룹 생성, 지출 등록 성공 등에서 코인 바운스(스프링 이징) 또는 스파크 파티클 연출
- **easing**: enter `cubic-bezier(.34,1.56,.64,1)`(오버슈트, 브랜드의 "통통 튀는" 톤), exit `ease-in`
- **duration**: micro 100ms, short 200ms, medium 350ms, 코인 바운스류 600ms

## 아이콘 / 일러스트 언어

- 로고의 코인(₩) 배지, 스파크(파열) 마크를 앱 전반의 "성공/완료" 아이콘 세트로 재사용
- 아이콘은 2px 이상 두꺼운 스트로크 + 둥근 선 끝(round line cap) — 얇고 각진 아이콘과 섞지 않음
- 카테고리 아이콘(숙소/식비/교통 등)은 배지 형태(원형 배경 + 흰색 아이콘)로 통일 — 기존 프로토타입의 `.cat-badge` 패턴과 호환됨

## 의사결정 로그

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-12 | 로고 기반 디자인 시스템 최초 수립 | 사용자가 제공한 "노나내" 로고(초록 지폐 + 코인 + 스티커 타이포)를 서비스 정식 로고로 채택하면서, 기존 프로토타입의 청록 중심 팔레트를 로고의 그린 브랜드 컬러로 승격 |
| 2026-09-12 | 서비스명 오기 정정 | 로고 텍스트를 "노나"로 잘못 읽어 초안에 반영했던 것을 "노나내"로 정정 (저장소명 `nonanae`와 일치) |
