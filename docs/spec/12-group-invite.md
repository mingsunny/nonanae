# 12. 멤버 초대 (그룹 내부)

> 앱 미가입 친구를 이름만으로 미리 추가해 정산에 포함시키는 기능. 대기 중 멤버 한 명당 개인화된 초대 링크를 보내서, 그 링크로 들어오면 매칭 화면 없이 바로 그 자리에 연결됨(07 삭제됨).

그룹 멤버 목록을 확인하고, 아직 앱에 가입하지 않은 친구도 이름만으로 미리 추가해 정산 계산에 포함시키는 화면.

- **담당자**:
- **상태**: 초안

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 화면 ID | 12_group_invite |
| 역할 | 멤버 목록 조회 + 대기 중(앱 미가입) 멤버 추가/일괄 초대 |
| 진입 경로 | 08/09/10 공용 헤더의 멤버 아바타 줄 탭(08 §3.1 참고), '멤버 초대' 버튼 |
| 다음 화면 | 카카오톡으로 초대 보내기 |

## 2. 진입 조건

- 08 §2와 동일한 그룹 멤버 조건

## 3. UI 구성

### 3.1 멤버 목록

- 아바타: 이름 첫글자 노출
- 이름: schema.md의 이름 결정 순서 참고(대기중=Member.name / 게스트=Member.nickname / 정식회원=User.name)
- 배지: 대기 중 멤버는 "초대 대기 중" 배지
- 대기 중 멤버는 그 옆에 "초대 보내기" 링크가 각각 붙음(멤버 한 명당 하나, 일괄 버튼 아님)

### 3.2 대기 중 멤버 추가

- 이름 입력하여 멤버 추가
  - 이름 (텍스트)
  - "추가" 버튼 클릭 시 멤버 목록에 추가

## 4. 기능

| 기능 | 동작 |
|---|---|
| 이름 입력 후 "추가" | 대기 중 멤버로 그룹에 추가, 목록 갱신, 토스트("{이름}님을 추가했어요") |
| 특정 멤버 옆 "초대 보내기" 클릭 | 그 멤버 전용 개인화 코드 생성 후 카카오톡 공유(또는 복사) — 이 코드로 들어오면 매칭 화면 없이 자동으로 그 자리에 연결됨 |
| 헤더 back | 그룹 상세로 복귀 |

## 5. 데이터

- 멤버 목록: `Group.members`(같은 그룹의 Member 전체)
- 대기 중 멤버 추가: 새 `Member` 행 생성
  - `name`에 입력한 이름 저장
  - `userId`는 null
- 개인화 초대 코드 형식: `{Group.inviteCode}-{Member.id}`
  - 이 코드로 들어오면 그 Member.id 자리에 자동 연결
  - 대기 중 멤버가 아니면 무시하고 신규 멤버로 처리

## 6. 예외 처리

| 상황 | 처리 |
|---|---|
| 이름 입력이 비어있음 | "추가" 버튼 비활성화 |

## 연관 화면

- [08-group-expense-list.md](08-group-expense-list.md)
- [11-expense-form.md](11-expense-form.md)

## 변경이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| v1.0 | 2026-09-10 | 프로토타입(`settlement-prototype-sep06-yunjung.html`) 기준 초안 작성 |
