# 정산 앱 (여행 경비 정산 서비스) — 기획 초안

생성일: 2026-08-23 / 업데이트: 2026-08-25 (열린 질문 3개 최종 결정 반영)
상태: DRAFT (경량 방식으로 작성, gstack CEO 리뷰 전체 프로세스는 건너뜀)

## 문제 정의

친구들과 여행 등을 다녀오면 여러 명이 여러 차례에 걸쳐 돈을 쓰게 되고, 누가 누구에게 얼마를 보내야 하는지 계산하기 번거롭다. 특히 일부 인원만 관련된 지출(예: 3명만 택시를 탄 경우)이 섞이면 수기 계산이 더 어려워진다.

## 핵심 기능 (확정된 범위)

1. **로그인 및 계좌정보 등록** — 카카오 로그인(v1), 가입 과정에서 계좌번호·은행명 필수 입력
2. **그룹(여행) 생성 및 초대** — 대표자가 그룹 생성 → 카카오톡 공유하기(자동 입장 링크) 또는 초대코드 직접 전달, 두 방식 모두 지원
3. **지출 항목 업로드** — 금액, 카테고리, 영수증 사진, 결제자, 참여자(서브셋 지정 가능) 기록
4. **최소 송금 정산 계산** — 그룹 내 지출을 바탕으로 "누가 누구에게 얼마" 송금하면 되는지 최소 건수로 제시
5. **정산 내역 확인** — 인당 결제금액/사용금액 탭 분리 확인, 카테고리별·전체 경비 요약
6. **빠른 송금** — 정산 결과에서 상대방 계좌번호를 롱프레스로 복사해 바로 붙여넣기

## 확정된 스코프 결정

| 결정 사항 | 선택 | 이유 |
|---|---|---|
| 로그인 | 카카오 소셜 로그인만 (v1) | 이메일 로그인은 비밀번호 저장·재설정 플로우·인증 메일 발송 인프라가 추가로 필요해 일정에 영향이 큼. 카카오 우선 출시로 MVP를 가볍게 유지하고, 이메일 로그인은 Phase 2로 미룸 |
| 계좌정보 등록 시점 | 로그인/가입 시 필수 입력 | 나중에 등록하게 하면 "송금 화면에서 계좌번호가 없는 사람" 예외 케이스가 생김. 가입 단계에서 강제하면 정산 화면은 항상 계좌정보가 있다고 가정 가능 |
| 그룹 초대 | 카카오톡 공유하기 + 초대코드 직접 입력, 두 방식 병행 | 카카오톡 공유는 원터치로 링크 열면 바로 입장(마찰 최소). 초대코드는 카카오톡을 안 쓰는 경우(문자, 다른 메신저 등)를 위한 범용 경로. 두 방식 모두 동일한 `invite_code`를 사용 — 공유 링크에 코드가 파라미터로 포함되고, 수동 입력 화면은 그 코드를 직접 타이핑 |
| 지출 카테고리 | 고정 카테고리 목록 사용 (식비/숙박/교통/쇼핑/액티비티/기타) 6개로 확정 | 커스텀 카테고리는 검색/필터 UI가 복잡해짐. 여행 경비는 이 6개로 대부분 커버되고, 프로토타입도 이미 이 6개로 구현되어 있어 추가 변경 없이 그대로 진행 |
| 영수증 사진 | 업로드 가능, 필수 아님 | 정산 근거 확인용. 필수로 하면 입력 마찰이 커서 선택 사항으로 둠 |
| 송금 방식 | 계좌번호 롱프레스 복사 (딥링크는 Phase 2 스트레치) | 사용자가 명시한 UX가 롱프레스 복사. 뱅킹앱 딥링크는 은행별 스킴이 제각각이라 v1에서는 복사만 확실히 지원 |
| 계좌 여러 개 등록 시 노출 | 정산/송금 화면엔 기본 계좌(`is_primary`) 1개만 노출 | 토스·카카오페이 등도 빠른 송금 화면엔 항상 기본 계좌 하나만 보여줌 — 계좌 선택 UI를 핵심 플로우에 넣으면 "롱프레스로 바로 복사"라는 목적과 어긋남. 계좌 전환은 "내 정보" 화면에서만 |
| 카카오 로그인 요청 동의항목 | **닉네임만** 요청 (프로필 사진·이메일·이름·성별·생일·전화번호 요청 안 함) | 서비스 기능(정산 계산, 계좌번호 표시)에 닉네임 외엔 실제로 필요한 정보가 없음 — 개인정보 최소수집 원칙에 맞고, 동의 화면 항목이 적을수록 로그인 승인율도 높아짐. `name`(실명) 동의항목은 카카오싱크 전용이라 사업자등록번호·사업자등록증 등 별도 심사가 필요해 v1(무사업자 개인 개발 단계)에서는 신청 자체가 불가 |
| 서비스 내 표시 이름 | 카카오 닉네임을 기본값으로 채우되, 온보딩 화면에서 사용자가 자유롭게 수정 가능 | 카카오 닉네임은 실명과 다를 수 있음(이모지·별명 등). 실명으로 보이길 원하면 사용자가 직접 고쳐 쓰면 되므로, 카카오싱크 없이도 "실명처럼 보이는 이름"을 달성 가능. 추후 사업자등록증을 내서 카카오싱크로 전환하면 `name` 동의항목으로 대체 가능 (아래 다음 단계 참고) |
| 플랫폼 | 모바일 웹 (반응형 웹앱) | 설치 없이 링크 공유만으로 친구 초대 가능, 앱스토어 심사 불필요 |

## 도메인 모델

```
User
 ├─ id, email (nullable, v1 미사용), kakao_id, name, profile_image
 ├─ auth_provider: kakao (v1 고정값. email 컬럼은 Phase 2 이메일 로그인 추가 시 마이그레이션 없이 확장하기 위해 미리 남겨둠)
 ├─ name: 최초 로그인 시 카카오 닉네임으로 초기화, 이후 사용자가 온보딩 화면에서 직접 수정 가능 (카카오 실명 동의항목이 아님 — 위 스코프 결정 참고)
 └─ primary_account: BankAccount (가입 시 필수 1개, 이후 추가 등록 가능)

BankAccount
 ├─ id, user_id, bank_name, account_number, account_holder
 └─ is_primary (정산 화면에 기본으로 보여줄 계좌)

Group (여행)
 ├─ id, name, created_by, invite_code (UNIQUE), created_at
 └─ members: GroupMember[]

GroupMember
 ├─ group_id, user_id, joined_at
 └─ role: owner | member

Expense (지출 항목)
 ├─ id, group_id, paid_by (user_id), amount, category, memo, receipt_image_url (nullable), spent_at
 └─ participants: ExpenseParticipant[]  ← 일부 인원만 지정 가능

ExpenseParticipant
 ├─ expense_id, user_id
 └─ (v1: 균등 분할만 지원. share_amount는 항상 amount / participants.length)

Category (enum, v1 고정값)
 └─ 식비 | 숙박 | 교통 | 쇼핑 | 액티비티 | 기타
```

**변경점 (이전 버전 대비)**
- 로그인은 v1에서 카카오 하나로 확정 (이메일 로그인은 Phase 2로 미룸). `User.email` 컬럼은 미리 남겨두되 v1에서는 사용하지 않음
- `BankAccount`에 `is_primary` 추가 — 여러 계좌 등록 가능하되 정산 화면엔 기본 계좌 하나만 노출로 확정
- `Expense`에 `category`, `receipt_image_url` 추가
- `Group`에 `invite_code` 추가 — 초대코드 방식 확정에 따른 필드

**중요 설계 결정 (유지):** `ExpenseParticipant`가 "이 지출에 관련된 사람"을 명시적으로 저장한다. 즉 지출당 참여자 서브셋을 자유롭게 지정할 수 있고, 정산 계산은 이 참여자 목록만 사용한다.

## 정산 알고리즘 (최소 송금 그래프)

1. **잔액 계산**: 그룹 멤버별로 `balance = 낸 금액 합 - 부담해야 할 금액 합`
   - 낸 금액 합 = 그 사람이 `paid_by`인 모든 지출의 `amount` 합 (= "각자 얼마 결제했는지")
   - 부담해야 할 금액 합 = 그 사람이 참여자로 지정된 모든 지출에서 각자의 `share`(아래 나머지 배분 규칙 참고)의 합 (= "각자 여행에서 전체 사용한 금액")
   - 이 두 값의 차이가 곧 `balance`이며, 05번 스펙의 "탭으로 분리해서 보여주는" 두 숫자가 바로 이 두 합계다.

**나머지(잔돈) 배분 규칙 — 결제자가 손해 보지 않는 구조 (확정)**

지출 금액이 참여자 수로 나누어떨어지지 않을 때, 결제자(돈을 받아야 하는 사람)가 반올림 때문에 덜 받는 일이 없도록 잔돈은 항상 결제자가 아닌 다른 참여자들이 나눠서 더 부담한다.

```
base = floor(amount / N)              // N = 참여자 수
remainder = amount - base * N         // 0 ≤ remainder < N

결제자 본인의 몫(참여자에 포함된 경우) = 항상 base (내림, 고정)
나머지 참여자 중 remainder 명 = base + 1
그 외 참여자 = base
```

- 배분 대상 순서는 결정론적으로 고정 (예: 결제자를 제외한 참여자를 `user_id` 오름차순 정렬 후 앞에서부터 remainder 명에게 1원씩 추가) — 매번 같은 지출을 다시 계산해도 같은 결과가 나와야 함
- 결제자가 그 지출의 참여자가 아닌 경우(자기는 안 먹었지만 결제만 한 경우)는 참여자 전원을 대상으로 동일 로직 적용 — 이 경우도 결제자는 자기 몫이 없으므로 자동으로 전액을 돌려받는 구조가 유지됨
- 예시: 10,000원을 A(결제자)/B/C 3명이 나눔 → base=3333, remainder=1 → A는 3333원 몫 고정, B·C 중 한 명이 3334원, 나머지 한 명이 3333원. A는 정확히 10000 - 3333 = 6667원을 돌려받아 반올림 손해가 전혀 없음
2. **부채 단순화 (Debt Simplification)**: 채권자(balance > 0)와 채무자(balance < 0)를 각각 정렬 후, 가장 큰 채권자와 가장 큰 채무자를 매칭해서 `min(|채권|, |채무|)`만큼 송금 관계를 만들고, 잔액을 0에 가깝게 줄여가며 반복.
   - 이 그리디 방식은 진짜 최소 거래 수를 보장하진 않지만(일반 케이스는 NP-hard), 실무에서 Splitwise 등도 쓰는 방식이고 사람이 보기에 충분히 "적은 거래"가 나온다.
   - 그룹 인원이 아주 많지 않은 이상(여행 그룹 특성상 보통 2~15명) 성능 문제 없음.
3. **카테고리별 집계**: `Expense.category`로 그룹핑해서 카테고리별 합계 산출 (05번 스펙 "카테고리 별로는 얼마나 지출했는지").
4. 결과: `[{from: user_id, to: user_id, amount}]` 리스트를 화면에 표시.

**엣지 케이스**
- 그룹 멤버가 1명뿐인 지출 → 정산 대상에서 제외 (자기 자신에게 보낼 필요 없음)
- 잔액이 이미 0인 멤버는 정산 리스트에 등장하지 않음
- 영수증 사진 업로드 실패/용량 초과 → 지출 등록 자체는 막지 않고 사진 없이 저장 후 재업로드 유도

## 데이터베이스 스키마 (초안)

```sql
users (id, email UNIQUE NULLABLE, kakao_id UNIQUE, auth_provider, password_hash NULLABLE, name, profile_image, created_at)
bank_accounts (id, user_id FK, bank_name, account_number, account_holder, is_primary, created_at)
groups (id, name, invite_code UNIQUE, created_by FK, created_at)
group_members (group_id FK, user_id FK, role, joined_at, PRIMARY KEY(group_id, user_id))
expenses (id, group_id FK, paid_by FK, amount, category, memo, receipt_image_url NULLABLE, spent_at, created_at)
expense_participants (expense_id FK, user_id FK, PRIMARY KEY(expense_id, user_id))
```

- 계좌번호는 민감 정보 — 같은 그룹 멤버에게만 노출, 그룹 멤버가 아닌 사용자의 API 접근은 반드시 차단 (v1에서는 컬럼 암호화까지는 과할 수 있으나 접근 제어는 필수)
- `email`, `password_hash`는 v1에서는 항상 NULL (카카오만 지원) — Phase 2에서 이메일 로그인을 추가할 때 이 컬럼들을 그대로 사용하도록 미리 만들어 둠
- `invite_code`는 추측 어렵게 충분히 긴 랜덤 문자열로 생성 (예: 8자리 영숫자) — 무차별 대입으로 다른 그룹에 침입 못 하도록

## 주요 화면 흐름

```
로그인 (카카오, v1)
  → [최초 가입 시] 온보딩: 이름, 계좌번호/은행명 입력 (필수)
  → 하단 전역 GNB: [홈] [프로필]  ─ 그룹 상세 화면을 포함해 로그인 이후 모든 화면에서 동일하게 노출
      [홈] = 내 그룹(여행) 목록
          → 그룹 생성 (이름 입력 → 초대코드 자동 생성 → 바로 그룹 상세로 진입, 별도의 "초대코드 발급 완료" 화면 없음)
          → 그룹 참가 (카카오톡 공유 링크로 자동 입장 또는 초대코드 직접 입력)
          → 그룹 상세 (상단 토글로 두 뷰 전환, 기본값은 지출)
              ├─ 지출 뷰 (기본 화면 — 그룹 내에서 가장 자주 하는 행동이라 우선 배치)
              │     ├─ (그룹 생성 직후에만) 초대코드 배너 — [카카오톡으로 초대하기] + 코드 복사 텍스트 링크, 닫기(✕) 가능
              │     └─ 지출 목록 + [지출 추가] (금액, 카테고리, 영수증 사진(선택), 결제자, 참여자 체크박스)
              └─ 정산 뷰 (구 "홈" 탭 + "정산" 탭 통합 — 둘 다 돈 얘기라 하나로 묶음)
                    ├─ "누가 누구에게 얼마" 최소송금 리스트 + 계좌번호 롱프레스 복사
                    └─ 인원별(결제액 vs 사용액) / 카테고리별 지출 합계 (토글로 전환)
              멤버 초대: 그룹 헤더의 멤버 아바타 줄 끝 "+" 칩 → 언제든 카카오 공유/코드 복사 재접근
      [프로필] = 이름/은행/계좌번호 확인 및 수정 (여러 계좌 등록은 Phase 2, v1은 기본 계좌 1개)
```

## 그룹 초대 흐름 (케이스별)

플랫폼이 네이티브 앱이 아니라 모바일 웹이라 "앱 다운로드" 단계 자체가 없다. 초대 경로는 카카오톡 공유(링크형)가 메인이고, 초대코드 직접 입력은 카카오톡을 안 쓰는 채널(문자, 다른 메신저, 구두 전달)을 위한 백업 경로다.

**케이스 A — 카카오톡 공유 링크 클릭 + 이미 로그인된 사용자**
1. 그룹원이 "카카오톡으로 초대하기"로 보낸 카드(링크: `.../join?code=XXXX`)를 받는다
2. 카드를 탭 → 브라우저가 열리며 URL에 초대코드가 이미 포함된 채로 랜딩
3. 로그인 세션이 살아있으므로 별도 입력 없이 그 코드로 즉시 그룹 참여 처리 → 그룹 홈으로 리다이렉트

**케이스 B — 카카오톡 공유 링크 클릭 + 처음 쓰는 사용자(로그인 안 됨)**
1. 케이스 A와 동일하게 링크 클릭 → `.../join?code=XXXX`로 랜딩
2. 로그인이 안 되어 있으므로, 코드를 세션/URL에 잠시 보관한 채 카카오 로그인 화면으로 이동
3. [최초 가입 시] 온보딩(이름/은행/계좌번호) 완료
4. 로그인 완료 직후, 보관해뒀던 코드로 자동 그룹 참여 처리 → 그룹 홈으로 리다이렉트
5. 사용자는 "초대코드 입력" 화면 자체를 볼 필요가 없음 — 전 과정에서 코드를 직접 타이핑하지 않음

**케이스 C — 초대코드를 텍스트로만 전달받음 (카카오톡 링크 공유가 아닌 경우)**
1. 그룹원이 "코드 복사하기"로 복사한 순수 텍스트 코드(예: `4B7K2P`)를 문자/다른 메신저/구두로 전달
2. 받는 사람은 서비스 URL을 별도로 알고 있어야 함 (링크가 아니므로 자동으로 랜딩되지 않음) — 전달하는 사람이 코드와 함께 서비스 URL도 같이 보내야 함
3. 접속 → 카카오 로그인 (+ 처음이면 온보딩)
4. "내 그룹" 홈에서 "초대코드 입력" 화면으로 직접 이동
5. 코드를 손으로 입력 → "그룹 참여하기" 클릭 → 그룹 홈으로 이동

케이스 A/B가 마찰이 훨씬 적으므로 UI상으로도 "카카오톡으로 초대하기"를 "코드 복사하기"보다 위/우선으로 배치한다 (프로토타입에 반영됨).

**그룹 생성자 쪽 UX (변경됨):** 그룹을 만든 사람은 "초대코드 발급 완료" 화면에서 "그룹으로 이동"을 눌러야 넘어가는 구조였는데, 이걸 없앴다. 카카오톡 공유를 누르면 카톡 앱으로 전환되는데, OS가 자동으로 우리 서비스로 복귀시켜주지 않기 때문에(공유 시트류 공통 특성) "공유 후 안 돌아오면 어떡하나"라는 문제가 있었음. 해결책은 그 화면 자체를 없애는 것 — 그룹 생성 즉시 그룹 상세(지출 뷰)로 들여보내고, 초대코드/카카오 공유는 지출 뷰 상단의 닫을 수 있는 배너로 노출한다. 이러면 "돌아와야 완료되는 단계"가 애초에 없어서, 카톡에 갔다가 언제 돌아오든 상관없다 (이미 그룹 안에 들어와 있음). 배너를 닫아도 그룹 헤더의 멤버 아바타 줄 끝 "+" 칩에서 언제든 동일한 카카오 공유/코드 복사에 다시 접근 가능. (기존엔 "멤버 초대"라는 별도 텍스트 링크가 상단에 있었는데, 배너와 기능이 겹쳐 보인다는 피드백으로 "+" 칩으로 대체함)

### 카카오톡 공유 구현에 필요한 것 (Kakao Login과는 별개 기능, 심사 불필요)

- **카카오 쪽 준비**: JavaScript 키 발급 (로그인용 REST API 키와는 다른 키), 카카오 디벨로퍼스 "플랫폼"에 실제 서비스 도메인 등록, 공유 카드용 썸네일 이미지(선택)
- **프론트엔드 구현**:
  1. `<script src="https://developers.kakao.com/sdk/js/kakao.js"></script>` 로드 + `Kakao.init(JS키)`
  2. "카카오톡으로 초대하기" 클릭 시 `Kakao.Share.sendDefault({ content: {title, description, imageUrl}, link: {mobileWebUrl, webUrl: '.../join?code=XXXX'}, buttons: [...] })` 호출
  3. **`/join?code=XXXX` 라우팅 로직** (실질적인 작업량이 여기 있음) — 로그인 여부에 따라 케이스 A/B 분기 처리
- 카카오 로그인과 달리 별도 동의항목 심사나 비즈앱 전환이 필요 없음 — 앱 등록 + 도메인 등록만으로 바로 사용 가능

## 기술 스택 추천

- **프론트엔드**: Next.js (React) — 반응형 웹앱, 카카오 SDK 연동 쉬움, Vercel 배포로 빠른 반복
- **백엔드**: Next.js API Routes (or 별도 Node/Express) — 초기 규모에선 풀스택 프레임워크로 충분
- **DB**: PostgreSQL (Supabase 추천 — 인증 연동, 무료 티어로 시작 가능)
- **파일 스토리지**: Supabase Storage — 영수증 사진 업로드용 (신규 요구사항)
- **인증**: NextAuth.js — Kakao Provider만 v1에 구성 (Credentials Provider는 Phase 2 이메일 로그인 시 추가)
- **배포**: Vercel (프론트+백엔드), Supabase (DB + Storage)

이 스택은 개인 프로젝트 규모에서 인프라 관리 부담을 최소화하면서 몇 주 내 MVP 완성이 가능하다. v1은 카카오 로그인만 지원해 비밀번호 재설정·이메일 인증 발송 같은 인프라를 구축할 필요가 없어 구현 범위가 가볍고, 영수증 업로드(파일 업로드 파이프라인)만 추가로 커버하면 된다.

## MVP 범위

**포함**
- 카카오 로그인 (v1)
- 가입 시 계좌번호/은행명 필수 입력
- 그룹 생성 + 카카오톡 공유하기 / 초대코드 직접 입력, 두 방식 병행 참가
- 지출 등록 (카테고리 6개 확정, 영수증 사진(선택), 참여자 서브셋 지정 포함)
- 정산 계산 및 표시 (결제자가 반올림 손해를 보지 않는 나머지 배분 규칙 적용)
- 정산 내역 탭 (결제금액/사용금액 분리, 카테고리별 요약, 총액)
- 계좌번호 롱프레스 복사 (기본 계좌 1개만 노출)

**명시적으로 제외 (스코프 아님, MVP도 Phase 2도 아님)**
- 정산 완료 체크(누가 실제로 보냈는지 추적) — 사용자 확인: 넣지 않기로 확정
- "지출 등록 확정" 후 정산 시작 알림 — 검토했으나 제외: (1) 여러 명이 비동기로 각자 입력하는 구조라 "확정"이라는 잠금 이벤트를 만들면 뒤늦게 지출을 추가하는 실제 상황과 계속 충돌함(잠금 해제/재확정 같은 엣지케이스가 늘어남), (2) 확정 없이 "정산 알림 보내기"만 넣는 것도 애매함 — 이 앱은 Splitwise처럼 지출 등록자 개인이 상환을 요청하는 구조가 아니라 N명의 지출을 합산해 그룹 단위로 최소 송금을 계산하는 구조라, "누가 이 알림을 보내는 주체인가"가 명확하지 않음. 결론: 정산 화면은 확정 이벤트나 알림 없이 항상 실시간으로 최신 지출 기준으로 재계산해서 보여준다. 이미 송금한 금액과 화면에 표시되는 최신 금액이 다를 수 있다는 리스크는 감수 — 그 리스크를 없애려면 결국 "정산 완료 체크"를 다시 만들어야 해서 위 항목과 동일한 이유로 제외

**제외 (Phase 2 이후로 미룸)**
- 이메일 로그인 (비밀번호 재설정, 이메일 인증 발송 포함)
- 커스텀 비율 분할 (현재는 균등 분할만)
- 다중 통화
- 앱 내 실제 계좌 이체 (PG/오픈뱅킹 연동)
- 뱅킹앱 딥링크 (v1은 복사까지만)
- 커스텀 카테고리 추가/편집
- 정산 화면에서 계좌 여러 개 중 선택 (v1은 기본 계좌 고정, 전환은 "내 정보"에서만)
- 네이티브 앱

## 결정된 질문

1. ~~나머지 배분 규칙~~ → **확정**: 결제자가 반올림 손해를 보지 않도록 잔돈은 다른 참여자가 흡수 (알고리즘 섹션 참고)
2. ~~정산 완료 추적 여부~~ → **확정**: 넣지 않음 (스코프 제외 목록으로 이동)
3. ~~그룹 초대 방식~~ → **확정**: 카카오톡 공유하기 + 초대코드 직접 입력 병행
4. ~~카테고리 최종 목록~~ → **확정**: 식비/숙박/교통/쇼핑/액티비티/기타 6개 (프로토타입과 동일, 추가 항목 없음)
5. ~~이메일 로그인 보안 요구 수준~~ → **확정**: v1은 카카오 로그인만 지원. 이메일 로그인(비밀번호 재설정, 인증 메일 발송 포함)은 Phase 2로 미룸
6. ~~계좌 여러 개 등록 시 UX~~ → **확정**: 정산/송금 화면엔 기본 계좌(`is_primary`) 1개만 노출. 계좌 전환은 "내 정보" 화면에서만 (토스·카카오페이 등 유사 서비스의 통상적인 패턴)
7. ~~카카오 로그인 시 실명(이름) 수집 여부~~ → **확정**: v1은 닉네임만 요청, 실명은 수집하지 않음. `name` 동의항목은 카카오싱크 전용이라 사업자등록이 필요해 지금은 신청 불가 — 온보딩에서 닉네임을 사용자가 직접 실명으로 고쳐 쓸 수 있게 하는 것으로 대체. 사업자등록증을 낸 뒤 카카오싱크로 전환하면 재검토

## 열린 질문

현재 없음 — 모든 열린 질문이 결정되어 스캐폴딩을 시작할 수 있는 상태.

## 다음 단계 제안

1. Next.js + Supabase 프로젝트 스캐폴딩 (Storage 버킷 설정 포함)
2. 정산 알고리즘 단위 테스트부터 작성 (엣지 케이스: 나머지 배분 확정 규칙, 1인 그룹, 이미 잔액 0인 멤버)
3. 카카오 로그인 연동 + 카카오톡 공유하기(Share) API 연동 (카카오 디벨로퍼스 앱 등록 필요 — 사용자가 직접 해야 하는 외부 계정 설정, 동의항목은 닉네임만 설정) + `/join?code=XXXX` 자동 참여 라우팅 구현 (위 "그룹 초대 흐름" 케이스 A/B 참고)
4. 프로토타입(`prototypes/settlement-prototype.html`)의 화면/카피/인터랙션을 그대로 Next.js 컴포넌트로 이식
5. (향후) 사업자등록증 발급 후 카카오싱크 전환 검토 — 실명(`name`) 동의항목, 이메일 등 추가 개인정보 수집이 필요해지면 그때 재평가
