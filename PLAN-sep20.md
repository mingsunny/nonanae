# 노나내 (여행 경비 정산 서비스) — 기획 (sep20)

생성일: 2026-08-23 (원본 `PLAN-sep03-yunjung.md`) / 이 문서: 2026-09-20
상태: 스캐폴딩 진행 중 (프론트 08~13 구현 완료, 백엔드는 Supabase)

> **이 문서의 범위**: "무엇을, 왜 그렇게 정했는가"(문제, 범위, 결정과 이유)만 다룬다.
> - **도메인 모델·DB 스키마·계산 로직은 [docs/spec/schema.md](docs/spec/schema.md)가 기준**이다 (DB는 `supabase/migrations/*.sql`이 최종 기준). 이 문서에는 옮겨 적지 않는다.
> - 화면별 동작은 `docs/spec/NN-*.md`, URL 경로·공통 규격은 [docs/spec/conventions.md](docs/spec/conventions.md)가 기준이다.
> - sep03 이후 바뀐 결정은 맨 아래 "결정 변경 이력"에 모았다. `PLAN-sep03-yunjung.md`는 이력으로 남겨두고 더 이상 갱신하지 않는다.

## 문제 정의

친구들과 여행 등을 다녀오면 여러 명이 여러 차례에 걸쳐 돈을 쓰게 되고, 누가 누구에게 얼마를 보내야 하는지 계산하기 번거롭다. 특히 일부 인원만 관련된 지출(예: 3명만 택시를 탄 경우)이 섞이면 수기 계산이 더 어려워진다.

## 핵심 기능 (확정된 범위)

1. **로그인 및 계좌정보 등록**
   - 그룹장은 정식 회원 필수: **이메일+비밀번호 회원가입/로그인만 지원** (구글·카카오 로그인은 스펙에서 제외)
   - 회원가입은 2단계: 이메일/비밀번호 → 이름·은행·계좌번호(필수)
   - 참가자는 정식 회원이거나, 로그인 없이 **이름만으로 참여하는 게스트** (PIN 등 별도 인증 없음)
   - 게스트는 계좌정보 없이 참여 (정식 회원으로 전환할 때 등록)
2. **그룹(여행) 생성 및 초대**
   - 그룹장이 그룹을 만들면 초대코드가 자동 생성됨
   - 초대 방법: 공용 초대코드/링크, 또는 **앱 미가입 친구를 이름으로 한 명씩 추가하고 각자에게 개인 초대 링크 발송**
3. **지출 항목 등록**
   - 금액, 카테고리, 영수증 사진(선택), 결제자, 참여자(서브셋 지정 가능), 나누기 방식(균등/비율/금액)
4. **최소 송금 정산 계산**
   - 그룹 지출을 바탕으로 "누가 누구에게 얼마"를 최소 건수로 제시
   - 내가 보낼 송금의 상대방 계좌번호를 **탭해서 복사**
5. **정산 내역 확인**
   - 카테고리별 비중, 인원별 결제 금액과 잔액(받을 돈/보낼 돈)

## 확정된 스코프 결정

| 결정 사항 | 선택 | 이유 |
|---|---|---|
| 로그인 | 이메일+비밀번호만. **구글·카카오 로그인은 제외** (Phase 2 후보도 아님) | 소셜 로그인은 계정 자동 연동 시 탈취 위험, 동의항목 확장 부담이 있어 v1에서 뺌. 대신 비밀번호 재설정·인증 메일 인프라가 v1부터 필요 |
| 참가자 참여 방식 | 그룹장은 정식 회원 필수. 참가자는 정식 회원 또는 **이름만으로 참여하는 게스트** (PIN 없음, 재입장 시 본인 확인 없음) | 여행 중 급하게 켜야 하는 상황, 계정 만들기를 꺼리는 참가자의 마찰을 없애기 위함. 같은 이름으로 다른 사람이 들어올 수 있는(스푸핑) 위험은 서로 아는 여행 그룹 맥락에서 감수 |
| 계좌정보 등록 시점 | 정식 회원은 가입 2단계에서 필수 입력. 게스트는 없음 | 송금 화면에서 "계좌 없는 수취인"이 정상 흐름에서 생기는 걸 정식 회원 쪽에서는 없앰. 게스트·대기 멤버가 받는 쪽이면 "등록된 계좌가 없어요" 문구로 대체하고 복사 UI를 끔 |
| 계좌 개수 | v1은 사용자당 계좌 1개 (은행, 계좌번호) | 정산 화면에 계좌를 하나만 보여주면 되고, 계좌 선택 UI가 "탭해서 바로 복사"라는 핵심 흐름과 어긋남. 여러 계좌는 Phase 2 |
| 그룹 초대 | 공용 초대코드/링크 + **대기 멤버 개인 초대 링크(`{초대코드}-{Member.id}`)** | 그룹장이 앱 미가입 친구를 이름으로 미리 추가해 지출을 기록해둘 수 있고, 그 친구가 개인 링크로 들어오면 매칭 화면 없이 바로 그 자리에 연결됨(`Member.id`가 유지돼 기존 지출 기록이 그대로 이어짐). 공용 코드로 들어오면 "본인이 누구인지 고르기" 화면을 거침 |
| 지출 카테고리 | 고정 6개: 숙소/식비/교통/액티비티/쇼핑/기타 | 커스텀 카테고리는 검색/필터 UI가 복잡해짐. 여행 경비는 이 6개로 대부분 커버됨 |
| 영수증 사진 | 업로드 가능, 필수 아님 | 정산 근거 확인용. 필수로 하면 입력 마찰이 커짐 |
| 나누기 방식 | 지출별로 균등/비율/금액 중 선택 | 일부 인원만 쓴 지출, 각자 다른 금액을 쓴 지출을 모두 커버 |
| 잔돈(나머지) 배분 | `equal`에서 남는 금액은 참여자 **전체**를 memberId 오름차순으로 정렬해 앞에서부터 1원씩. 결제자도 예외 없음 | 같은 지출을 다시 계산해도 항상 같은 결과가 나오는 결정론적 규칙이고, 스펙([schema.md](docs/spec/schema.md) "계산 로직")·코드와 일치함. 대가로 결제자가 최대 1원 덜 받을 수 있는데, 여행 정산에서 감수 가능한 오차로 판단 |
| 송금 방식 | 계좌번호 **탭해서 복사** (뱅킹앱 딥링크는 Phase 2) | 은행별 딥링크 스킴이 제각각이라 v1은 복사만 확실히 지원 |
| 정산 결과 표시 | 확정·알림 없이 항상 최신 지출 기준으로 실시간 재계산. 별도 요약 줄 없음 | 여러 명이 비동기로 입력하는 구조에서 "확정"이라는 잠금은 뒤늦은 지출 추가와 충돌함 |
| 그룹 내부 메뉴 | 그룹 안에서는 하단 탭바가 지출/정산/요약으로 바뀜. 그룹 밖은 홈/프로필 | 08~10을 하나의 레이아웃(공용 헤더 + 하단 탭바)에서 콘텐츠만 교체 |
| 멤버 초대 진입점 | 그룹 헤더에 "멤버 초대"(→ 12 멤버 목록·추가 화면), "초대 코드 복사"(즉시 복사) 상시 노출. 그룹 생성 직후에는 12 화면으로 이동하고 "그룹으로 가기"가 유일한 출구 | 초대를 상시 발견할 수 있게 하되, 초대 UI를 한 화면으로 통합 |
| 서비스 내 표시 이름 | 정식 회원은 가입 시 직접 입력(프로필에서 수정 가능), 게스트·대기 멤버는 그룹에 입력된 이름 | 소셜 프로필 자동 채움이 없어져 항상 직접 입력 |
| 플랫폼 | 모바일 웹 (반응형 웹앱) | 설치 없이 링크 공유만으로 초대 가능, 앱스토어 심사 불필요 |

## 도메인 모델과 계산 규칙

**[docs/spec/schema.md](docs/spec/schema.md)가 기준이다.** 엔티티(User, Group, Member, Expense, ExpenseParticipant, Notification), 필드, 계산 로직은 그쪽만 갱신한다. 여기서는 설계 원칙만 남긴다.

- 게스트와 대기 멤버는 `User` 없이 `Member`(`userId` null + `name`)로만 존재한다. 지출의 결제자·참여자는 항상 `Member.id`를 참조한다.
- `ExpenseParticipant`가 "이 지출에 관련된 사람"을 명시적으로 저장하고, 정산은 이 참여자 목록만 사용한다 (지출마다 서브셋 지정 가능).
- 정산 계산은 프론트에서 수행한다 (그룹이 2~15명 규모라 성능 문제 없음). 순수 함수로 분리해 단위 테스트한다.
- 계좌번호는 민감 정보다. 같은 그룹 멤버에게만 노출하고 그룹 멤버가 아닌 사용자의 접근은 막는다 (Supabase RLS).

**엣지 케이스** (동작은 [09](docs/spec/09-group-settle.md)·[10](docs/spec/10-group-summary.md) 참고)
- 멤버가 1명뿐이거나 잔액이 모두 0이면 정산 리스트가 비어 "정산할 금액이 없어요"를 표시
- 영수증 업로드 실패/용량 초과 → 지출 등록은 막지 않고 사진 없이 저장한 뒤 재업로드 유도

## 주요 화면 흐름 (요약)

```
앱 실행 → 인트로 → [로그인] / [회원가입 1단계 → 2단계(이름·은행·계좌)] / [초대코드로 가입하기]
  → 하단 메뉴: [홈] [프로필]
      [홈] = 내 그룹 목록 → 그룹 생성(→ 12 멤버 초대 → 그룹으로 가기) / 초대코드 입력
          → 그룹 상세 (하단 탭바: 지출 / 정산 / 요약, 기본은 지출)
              ├─ 지출: 날짜별 지출 목록 + [＋]. 항목 탭 → 수정 모드(하단에 삭제 링크)
              ├─ 정산: 내가 보낼 송금 먼저(금액 내림차순), 나머지는 이름순. 내 송금만 탭해서 계좌 복사
              └─ 요약: 카테고리별 비중 → 인원별 결제·잔액
              헤더: 멤버 아바타 줄(탭 → 12 멤버 목록), "멤버 초대", "초대 코드 복사"
      [프로필] = 이름/은행/계좌 확인·수정, 하단에 로그아웃/회원 탈퇴(저강조 텍스트 링크)
```

알림(13)은 홈 위에 뜨는 전역 오버레이로, 지출 등록과 멤버 참여 이벤트를 보여준다. 화면별 상세와 URL은 각 스펙과 conventions.md를 본다.

## 그룹 초대·참여 흐름

플랫폼이 모바일 웹이라 "앱 설치" 단계가 없다. 자세한 화면 동작은 [06](docs/spec/06-join-group.md), [07](docs/spec/07-join-match.md), [12](docs/spec/12-group-invite.md)를 본다.

**A. 개인 초대 링크 (대기 멤버 한 명당 하나)**
1. 그룹장이 12 화면에서 앱 미가입 친구를 이름만으로 추가 (`Member`, `userId` null → "초대 대기 중")
2. 그 멤버 옆 "초대 보내기"로 개인 링크(`/join?code={초대코드}-{Member.id}`)를 복사·공유
3. 친구가 링크로 들어오면 매칭 화면 없이 그 자리에 바로 연결됨. 로그인 상태면 내 계정이 그 자리에 붙고, 비로그인이면 게스트로 그 자리가 지정됨
4. 그 멤버가 이미 등록해둔 지출 기록이 그대로 이어짐 (`Member.id` 유지)

**B. 공용 초대코드/링크**
1. 코드(`ABCDEF`)를 입력하거나 링크(`/join?code=ABCDEF`)로 진입 (카카오톡 안 쓰는 채널은 코드를 텍스트로 전달, 서비스 URL도 함께 전달 필요)
2. "참여하기" 화면에서 그룹 멤버 목록 중 본인을 고르거나, 목록에 없으면 새로 참여 (비로그인이면 이름 입력, 로그인이면 내 계정으로 새 멤버 추가)
3. 이미 계정이 연결된 멤버는 선택할 수 없음 ("이미 가입됨")

**C. 재입장**
- 이미 그룹 멤버인 사람이 다시 들어오면 안내 없이 바로 그룹 내부로 이동
- 게스트는 다른 기기·브라우저에서 들어오면 같은 이름을 다시 골라 그 자리로 돌아옴 (본인 확인 없음)

**D. 게스트 → 정식 회원 전환**
- 회원가입 흐름을 재사용하고, 서버가 그 게스트 세션이 참여한 멤버 행을 새 계정에 연결함. `Member.id`가 유지돼 지출 기록을 옮길 필요가 없음 (`upgrade_guest`, [schema.md](docs/spec/schema.md) 참고)

**카카오톡 공유 구현 시 필요한 것** (카카오 로그인과 별개 기능, 심사 불필요)
- 카카오 디벨로퍼스에서 JavaScript 키 발급 + 실제 서비스 도메인 등록
- 프론트에서 카카오 SDK 로드 후 "카카오톡으로 초대하기"에서 `Kakao.Share.sendDefault`로 초대 링크 공유
- `/join?code=...` 라우팅이 실질적인 작업량

## 기술 스택

> 2026-09-20 팀 협의로 확정. 화면 담당은 01~07·14번 민선, 08~13번 kady.

- **프론트엔드**: React + Vite + TypeScript, 라우팅은 React Router, 스타일은 CSS Modules, 상태관리는 zustand
- **백엔드/DB**: Supabase (PostgreSQL). 담당: 민선
- **인증**: Supabase Auth 이메일+비밀번호. 게스트는 Supabase 익명 로그인 세션을 `members.guest_uid`에 연결해 구현 ([schema.md](docs/spec/schema.md) "DB 구현")
- **파일 스토리지**: Supabase Storage (영수증 사진)
- **정산 계산**: 프론트에서 수행 (서버 없음)
- **배포**: 프론트 배포 대상 미정

## MVP 범위

**포함**
- 이메일+비밀번호 회원가입(2단계)/로그인, 비밀번호 재설정
- 가입 시 이름·은행·계좌번호 필수 입력
- 그룹 생성, 공용 초대코드/링크, 대기 멤버 추가 + 개인 초대 링크
- 게스트 참여 (이름만으로, 재입장 시 본인 확인 없음)
- 지출 등록/수정/삭제 (카테고리 6개, 영수증 사진 선택, 참여자 서브셋, 균등/비율/금액)
- 정산 계산 및 표시, 내 송금 계좌 탭해서 복사
- 요약 (카테고리별, 인원별 결제·잔액)
- 알림 (지출 등록, 멤버 참여)
- 로그아웃 / 회원 탈퇴

**명시적으로 제외 (MVP도 Phase 2도 아님)**
- 구글·카카오 로그인 연동
- 게스트 닉네임+PIN 재입장 인증
- 정산 완료 체크 (누가 실제로 보냈는지 추적)
- "지출 등록 확정" 후 정산 시작 알림 — 여러 명이 비동기로 입력하는 구조와 충돌하고, 알림의 주체가 불분명함. 정산 화면은 항상 실시간 재계산하며, 이미 송금한 금액과 최신 금액이 다를 수 있는 리스크는 감수

**Phase 2 이후**
- 다중 통화
- 앱 내 실제 계좌 이체 (PG/오픈뱅킹)
- 뱅킹앱 딥링크
- 커스텀 카테고리
- 여러 계좌 등록·선택
- 네이티브 앱

## 결정된 질문

1. ~~잔돈 배분 규칙~~ → **확정 (2026-09-20 변경)**: 참여자 전체 memberId 오름차순 (스코프 결정표 참고)
2. ~~정산 완료 추적 여부~~ → 넣지 않음
3. ~~그룹 초대 방식~~ → 공용 코드/링크 + 대기 멤버 개인 링크
4. ~~카테고리 목록~~ → 숙소/식비/교통/액티비티/쇼핑/기타
5. ~~계좌 여러 개~~ → v1은 1개
6. ~~커스텀 비율 분할~~ → v1 포함 (균등/비율/금액 3방식을 폼에서 인라인 토글)
7. ~~참가자 비로그인 참여~~ → 이름만으로 참여, PIN 없음
8. ~~로그인 수단~~ → 이메일+비밀번호만

## 열린 질문

- 이메일 인증을 가입의 필수 조건으로 할지 ([01-login.md](docs/spec/01-login.md), `schema.md`에서도 미정)
- 인증·비밀번호 재설정 메일 발송 방식 (Supabase Auth 기본 메일 사용 여부)과 프론트 배포 대상 — 민선이 정함
- 게스트가 정식 회원으로 전환하는 진입 지점 UI ([01-login.md](docs/spec/01-login.md)에서 미정)
- 같은 게스트가 세션을 잃고 다시 들어와 중복 멤버가 생기는 경우의 처리 ([07-join-match.md](docs/spec/07-join-match.md) 예외처리에 기록됨, 정책 결정 없음)

## 다음 단계

1. Supabase 프로젝트 설정 (스키마 마이그레이션은 `supabase/migrations/`에 있음, Storage 버킷 포함)
2. 프론트의 목업 데이터 계층(`app/src/api/`)을 Supabase 호출로 교체
3. 이메일+비밀번호 회원가입/로그인 구현 (비밀번호 재설정, 인증 메일 포함)
4. `/join?code=...` 라우팅과 개인 초대 링크 자동 매칭 구현
5. 카카오톡 공유하기 연동 (카카오 디벨로퍼스 앱 등록·도메인 등록은 직접 해야 하는 외부 계정 설정)
6. 01~07·14번 화면 구현 (민선). 08~13번은 구현 완료

## 테스트 계정 (sep10 프로토타입 기준)

- **로그인 테스트 계정**: `a@naver.com` / `aaaaaaaa` (이름: 테스트, 계좌 카카오뱅크 3333-01-9999999)
- **초대코드 테스트**: 초대코드 `TEST42`로 "초대코드 테스트방" 그룹이 미리 만들어져 있음. 계정 없는 대기 멤버 "김민지"가 있어 로그인/비로그인 양쪽에서 "나 고르기" 흐름을 테스트할 수 있음

## 결정 변경 이력 (sep03 대비)

| 날짜 | 항목 | 이전 | 현재 | 이유 |
|---|---|---|---|---|
| 2026-09-06 | 로그인 수단 | 카카오 단독 | 이메일+비밀번호 기본 + 구글·카카오 연동 | sep03에 이유 기록 없음 (바로 아래 09-12에 다시 변경됨) |
| 2026-09-12 | 로그인 수단 | 이메일 + 구글·카카오 연동 | **이메일+비밀번호만** | sep10 프로토타입 기준. 소셜 연동의 계정 탈취 위험·동의항목 부담을 지지 않기로 함 |
| 2026-09-12 | 게스트 인증 | 닉네임+PIN(5회 실패 15분 잠금) | **이름만, PIN 없음** | sep10 프로토타입 기준. 마찰을 더 줄임. 스푸핑 위험은 감수 |
| sep10~sep19 | 초대 방식 | 공용 코드/링크만 | + **대기 멤버 개인 초대 링크** | 앱 미가입 친구를 미리 추가해 지출을 기록해둘 수 있게 함 (정확한 도입 시점은 기록 없음) |
| 2026-09-20 | 계좌 복사 | 롱프레스 | **탭해서 복사** | 스펙 09·코드가 탭 방식이고, 롱프레스가 아니라 클릭해서 복사하는 것으로 확인함 |
| 2026-09-20 | 로그인 첫 화면 | 로그인 폼 | 인트로 → 로그인/회원가입/초대코드 | sep19 프로토타입 기준 |
| 2026-09-20 | 회원가입 | 폼 1개 | 2단계(이메일·비밀번호 → 이름·은행·계좌) | sep19 프로토타입 기준 |
| 2026-09-20 | 그룹 내부 메뉴 | 전역 [홈][프로필] 유지 + 상단 토글 3뷰("통계") | 그룹 안에서는 하단 탭바 지출/정산/요약 | 스펙 08 §3.2, 코드 기준 |
| 2026-09-20 | 멤버 초대 진입점 | 헤더에 "카카오톡 초대"/"코드 복사"가 즉시 실행 | "멤버 초대"는 12 화면으로 이동, "초대 코드 복사"만 즉시 실행. 그룹 생성 직후 12 화면 경유 | 스펙 08·12, 코드 기준 |
| 2026-09-20 | 정산 상단 요약 줄 ("내가 보낼 돈 · 총 N건") | 있음 | **없음** | 필요 없다고 판단 |
| 2026-09-20 | 계좌 없는 수취인 | 가입 시 필수라 정상 흐름에서 발생 안 함 | 게스트·대기 멤버가 받는 쪽이면 정상 흐름에서도 발생, "등록된 계좌가 없어요" 문구로 대체 | 게스트 도입 |
| 2026-09-20 | 잔돈 배분 (`equal`) | 결제자 몫은 항상 base(내림), 나머지 참여자가 1원씩 부담 | **참여자 전체를 memberId 오름차순으로 1원씩** (결제자 예외 없음) | 스펙·코드와 일치시킴. 결제자가 최대 1원 손해 보는 것은 수용 |
| 2026-09-20 | 카테고리 | 숙박 | **숙소** | 스펙·sep19 프로토타입·코드 기준 |
| 2026-09-20 | 도메인 모델·DB 스키마·계산 로직 | 이 문서에 기재 | **schema.md 기준, 이 문서에서 제거** | 스펙과 이중 관리되어 어긋남 |
| 2026-09-20 | 프론트 스택 | Next.js + API Routes | React + Vite + TypeScript, CSS Modules, zustand, Supabase | 팀 협의 |
